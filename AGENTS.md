# AGENTS.md — Stock Tech Blog

本リポジトリは、AIエージェントと連携して技術記事を執筆・公開する**公開**リポジトリである。
普段は動作しないが、業務中に技術的な発信・共有に値する内容を検出した場合、指示に基づき記事草案を作成・コミットする。

> ⚠️ **本リポジトリは public である。草案も、PR を作った時点で誰でも読める。**
>
> Draft PR は「マージ準備ができていない」という意味であって、内容を隠すものではない。
> したがって **`articles/drafts/` に置いてよいのは「公開すると決めたもの」だけ**である。
>
> 出すかどうかを決めていないネタ、公開留保がかかったもの、取り下げたものは、
> 非公開リポジトリで別管理する。詳細は §2「ネタの置き場」を参照。

---

## 1. リポジトリの目的

| 目的 | 詳細 |
|---|---|
| **メディア発信力の向上** | Stock社としての技術的プレゼンスを確立する |
| **採用力の向上** | エンジニア候補に対して技術力・開発文化を可視化する |
| **エンジニアの発信の場** | チームメンバーが技術的知見を社外に発信する基盤を提供する |

## 2. ディレクトリ構成

```
cto-tech-blog/                 # 本リポジトリ（public・Zenn 連携先）
├── AGENTS.md              # 本ファイル（AIエージェント向け運用ガイド）
├── README.md              # リポジトリ概要
├── PUBLISHING.md          # Zenn 公開フロー（変換・レビュー・公開の手順）
├── articles/
│   ├── <slug>.md          # Zenn が読む記事の正本（Zenn形式 frontmatter）
│   ├── drafts/            # 草案（執筆中・レビュー前）
│   │   └── YYYY-MM-DD_slug.md
│   └── published/INDEX.md # 公開済み記事の索引（記事ファイルは移動しない）
├── images/                # 記事に貼る画像
├── config/zenn.json       # Zenn 変換設定（publication 名・topics 対応表・著者フッタ）
├── scripts/               # draft_to_zenn.py / validate_zenn.py / update_index.py / sync_stage.py
└── templates/article_template.md
```

### ネタの置き場

**書くと決める前のネタは、本リポジトリに置かない。** public なので、`articles/drafts/` に入れた時点で題材が外から見える。「この領域の記事を準備している」という事実自体が、未リリース機能の先読み材料になりうる。

そのため次の3つは非公開リポジトリで別管理する。

| 対象 | 例 |
|---|---|
| まだ書くと決めていないネタ | 業務中に「記事にできそう」と拾っただけのもの |
| 公開留保がかかったもの | 解禁条件（リリース後、日付など）を満たすまで寝かせるもの |
| 取り下げたもの | 書いたうえで「出さない」と判断したもの |

**書くと決めた時点で、本リポジトリの `articles/drafts/` に移す。** 以降は Draft PR で進めてよい（内容は公開されるが、公開すると決めたものなので問題にならない）。

公開可否の判定基準（どういう情報が誰の役に立ってしまうか）と、著者の文体判定の定義も非公開リポジトリにある。判定基準そのものが自社の弱点の所在を示すため、公開対象に含めていない。

### ステージフロー

```
【非公開】ネタ ──→ drafts/ → articles/<slug>.md（published: false）→ PRでZenn下書き・レビュー → published: true で merge
   ネタ出し  書くと決める  草案作成   Zenn形式へ変換                体裁・校正はZennのレビュー機能        公開
```

- **昇格は移動であって複製ではない**: 非公開リポジトリの `ideas/` から `drafts/` へ上げたら、`ideas/` 側のファイルは削除する
- **drafts/**: 記事として構成・執筆した草案。本リポジトリ独自の frontmatter（§4）を使う。**ここに置く＝公開すると決めた**、を意味する
- **articles/ 直下**: Zenn 形式に変換した記事の正本。`scripts/draft_to_zenn.py` で生成する。**Zenn が読むのはここだけ**（サブディレクトリは走査されない）
- **published/INDEX.md**: 公開済み記事の索引。**公開後に記事ファイルを移動・削除してはならない**（Zenn 側の記事はファイル削除では消えず、逆にファイルが残ると復活する）
- **公開留保がかかったら drafts/ から引き上げる**: 留保の理由と再開手順を記録して非公開リポジトリへ戻し、PR は閉じる

> 変換・レビュー・公開の具体手順は [PUBLISHING.md](./PUBLISHING.md) を正とする。

## 3. ファイル命名規則

```
YYYY-MM-DD_slug.md
```

| 要素 | 説明 | 例 |
|---|---|---|
| `YYYY-MM-DD` | 記事作成日（アイデア起案日 or 草案着手日） | `2026-06-23` |
| `slug` | 記事を一意に識別する英数字スラッグ | `ai-agent-daily-report-automation` |

例: `2026-06-23_ai-agent-daily-report-automation.md`

`articles/` 直下（Zenn 記事）は**ファイル名がそのまま記事URLになる**ため、日付を付けず slug のみとする。
Zenn の制約により `a-z0-9` とハイフン・アンダースコアの **12〜50文字**。

```
<slug>.md   例: ecs-task-definition-rollback-illusion.md
```

## 4. 記事 Frontmatter 仕様

各記事ファイルの先頭に YAML frontmatter を配置する。

```yaml
---
title: "記事タイトル"
slug: "url-friendly-slug"
author: "著者名"
author_github: "github_username"
date: "YYYY-MM-DD"
updated: "YYYY-MM-DD"
stage: "idea | draft | review | published"
tags:
  - タグ1
  - タグ2
category: "カテゴリ"
description: "記事の概要（1-2文）"
source_context: "この記事のきっかけとなった業務・技術的背景"
published_url: ""  # 公開後に記入
---
```

### stage の遷移

| stage | 意味 | 場所 |
|---|---|---|
| `idea` | ネタ・アイデア段階 | 非公開リポジトリの `articles/ideas/`（§2） |
| `draft` | 草案として執筆中 | `articles/drafts/` |
| `review` | CTOレビュー中 | `articles/drafts/`（stage のみ変更） |
| `published` | 公開済み | `articles/` 直下（ファイルは移動しない。索引のみ `published/INDEX.md` に追記） |

### Zenn 記事の frontmatter（`articles/` 直下）

`articles/` 直下のファイルは **Zenn の仕様に従う**。§4 の草案 frontmatter とは別物であり、`scripts/draft_to_zenn.py` が変換する。手書きしない。

```yaml
---
title: "記事タイトル"
emoji: "🚢"                 # アイキャッチ絵文字1文字
type: "tech"                # tech: 技術記事 / idea: アイデア記事
topics: ["aws", "ecs"]      # 最大5件
published: false            # true になった状態で main に merge されると公開される
publication_name: "stock_inc"
---
```

| 草案側 | Zenn側 | 変換 |
|---|---|---|
| `title` | `title` | そのまま |
| `category` | `emoji` / `type` | `config/zenn.json` の対応表 |
| `tags` | `topics` | `config/zenn.json` の `tag_to_topic`、5件まで |
| `slug` | ファイル名 | `a-z0-9 - _` の12〜50文字 |
| `stage` / `source_context` | （本文冒頭のHTMLコメント） | Zenn では非表示 |

> ⚠️ **AIエージェントは `published: true` への変更を行わない。** 公開はレビュー承認後に人が実施する（§8-4）。

## 5. AIエージェントの運用ルール

### 5.1 記事ネタの検出基準

> 📌 **ネタの発掘・選別の方法論は [IDEA_SOURCING.md](./IDEA_SOURCING.md) を正とする。**
> 情報源（セッション履歴 / 会議文字起こし / 閲覧履歴）の使い分け、抽象化の線引き、
> 優先度の4軸、記事レンジのバランスはすべて同ファイルに定義されている。
> 本節はその中の「どのカテゴリに該当するか」の判定表である。

業務中に以下のいずれかに該当する技術的要素を検出した場合、記事ネタとして非公開リポジトリの `articles/ideas/` に起票する（§2）。

| カテゴリ | 判定基準 | 例 |
|---|---|---|
| **技術的課題解決** | 非自明な方法で技術的課題を解決した | N+1問題の体系的解決、パフォーマンスチューニング |
| **アーキテクチャ判断** | 設計上の重要な意思決定を行った | マイクロサービス分割、DB設計の方針転換 |
| **ツール・プロセス改善** | 開発プロセスやツールを改善した | CI/CD パイプラインの最適化、AIエージェント活用 |
| **障害対応・ポストモーテム** | 本番障害の原因分析と対策が完了した | メモリリーク調査、データ不整合の修復 |
| **新技術導入** | 新しい技術・ライブラリを本番導入した | OTel導入、新しいAWS サービスの活用 |
| **チーム・組織** | エンジニア組織の運用に関する知見 | コードレビュー文化、リモートワーク運用 |
| **AIエージェント活用** | AIエージェントの業務活用事例 | デイリーレポート自動生成、設計レビュー自動化 |

### 5.2 記事作成フロー

```
1. ネタ検出
   └─ AIエージェントが業務中に検出 or ユーザーが指示
   └─ 非公開リポジトリの articles/ideas/ にアイデアファイルを作成

2. 草案作成（ユーザーの指示があった場合のみ）
   └─ ideas/ のファイルを drafts/ に昇格
   └─ templates/article_template.md に基づき本文を構成
   └─ stage を "draft" に更新

3. レビュー
   └─ ユーザー（CTO）がレビュー・フィードバック
   └─ stage を "review" に更新

4. Zenn形式へ変換（ユーザーの指示があった場合のみ）
   └─ python3 scripts/draft_to_zenn.py <草案> → articles/<slug>.md（published: false）
   └─ python3 scripts/validate_zenn.py でエラー0を確認

5. 公開（人が実施）
   └─ published: false のまま PR → 内容レビュー + 体裁・校正レビュー
   └─ 承認後に published: true へ変更して main に merge = 公開
   └─ articles/published/INDEX.md に公開URLを追記
   └─ 草案の stage を "published"、published_url を記入（sync_stage.py）
```

### 5.3 記事の品質基準

#### 必須要件

- [ ] タイトルが具体的で、読者が得られる知見が明確である
- [ ] 「課題 → アプローチ → 結果 → 学び」の構造になっている（**テンプレートの見出しをそのまま使わない**。記事固有の見出しを立てる）
- [ ] **主張の強い記事には「想定される反論」と、それへの回答がある**（判定は下記）
- [ ] 選択をした記事には、**捨てた案とその理由**が書かれている（voice.md 公理A10）
- [ ] 技術的な正確性が担保されている（コード例があれば動作確認済み）
- [ ] Stock社固有の機密情報（顧客名・内部数値・非公開仕様）を含まない
- [ ] 読者（外部エンジニア）にとって再現可能な形で記述されている

#### 推奨事項

- [ ] コード例やコマンド例を含む（抽象論だけでなく具体性がある）
- [ ] 図やダイアグラムで視覚的に説明している
- [ ] 類似の課題に対する既存のアプローチとの比較がある
- [ ] 「なぜこの方法を選んだか」の意思決定プロセスが記述されている

### 5.4 機密情報ガード

> 📌 **本節を通過しても、公開可能とは限らない。**
> 本節は「誰の情報か」（顧客・個人・内部数値）で分類する関門である。
> これに加えて「誰の役に立つか」（競合・攻撃者・不安な顧客）で分類する競合ガードを、
> [ontology/disclosure.md](./ontology/disclosure.md) の判定手続きで**必ず通すこと**。

記事作成時に以下の情報が含まれていないことを**必ず確認する**。

| NG情報 | 例 |
|---|---|
| **顧客固有情報** | 顧客名、契約内容、問い合わせ内容 |
| **内部数値** | 売上、MRR、ユーザー数、具体的なサーバー台数 |
| **非公開仕様** | セキュリティ実装の詳細、認証フローの具体的実装 |
| **APIキー・認証情報** | 環境変数名は可、値は不可 |
| **社内URL** | Backlog、Stock、Slack等の内部URL |
| **個人情報** | 社員のフルネーム（GitHub アカウント名は可） |

> ⚠️ 判断がつかない場合は**含めない**。CTO レビュー時に個別判断する。

### 5.4.1 「想定される反論」の要否判定

次のいずれかに当てはまれば、**「想定される反論」の節が必須**である（`voice:CounterArgument` = `required: conditional`）。

| 判定 | 例 |
|---|---|
| 読者が**反対の立場を取れる主張**が本文にある | 「刻む」「標準に寄せる」「完成物を渡すな」「必須要件から外した」 |
| 一般に推奨されている方法を**採らなかった** | 定石を退けた、ベストプラクティスに従わなかった |
| コストや安全性の**トレードオフを引き受けた** | 権限を渡す、部分実装にする、監視の範囲を狭める |

**反論への回答はAIが書いてはならない。** 判断の中身は著者しか持っておらず、AIが書くと必ず一般論になる（公理 §8-8）。
AIがやるのは**論点を立てて著者に問い返すところまで**である。

観測された事実として、11本の記事のうち**8本でこの節が欠けていた**。口語では相手が反論してくれるため、
文章に移したときにだけ必要になる特性であり、意識しないと必ず落ちる（`voice:ElicitingQuestion` / `voice:CS_Confirmation`）。

### 5.5 記事のトーン・スタイル

| 項目 | ガイドライン |
|---|---|
| **文体** | です・ます調。技術的に正確かつ親しみやすい文体。**AI生成物に見える文体は不可** — 判定は [ontology/voice.md](./ontology/voice.md) に従う |
| **読者想定** | 中〜上級のWebエンジニア |
| **長さ** | 2,000〜5,000文字（目安） |
| **構成** | 見出し（h2/h3）を適切に使い、スキャンしやすくする |
| **コード例** | 実際に動くコードを掲載。言語・フレームワークのバージョンを明記 |
| **強調** | **本文の太字は主張2箇所まで**（`ai:TableOveruse`）。表のセルと段落先頭の連番ラベル（`**1. 事前告知**`）は構造表示なので対象外。まとめ節の箇条書きで再強調しない |
| **表** | 比較があるときだけ使う |
| **CTO視点の記事** | 技術選定の背景・組織的な判断理由を重視する |

## 5.6 Zenn 記事の制約（執筆時に効くもの）

`articles/` 直下の記事は Zenn がそのまま描画する。書いた後に気づくと手戻りになるものを挙げる。

| 制約 | 影響 | 対処 |
|---|---|---|
| **HTMLを解釈しない** | `<!-- コメント -->` が読者にそのまま表示される | 記事本文にHTMLコメントを書かない。`validate_zenn.py` がエラーで止める |
| **引用も表示される** | レビュー用メモ・公開留保メモが読者に見える | メモは草案側に置く。変換時に `draft_to_zenn.py` が除去する |
| `topics` は5件まで | 6件目以降は切り捨て | 変換時に警告が出る。`--topics` で明示指定する |
| ファイル名 = 記事URL | `a-z0-9` とハイフン・アンダースコアの12〜50文字 | slug をこの制約で決める |
| 削除はダッシュボードからのみ | ファイルを消しても記事は残り、残っていると復活する | 公開後にファイルを移動・削除しない |

## 6. 想定する記事カテゴリ・タグ体系

### カテゴリ

| カテゴリ | 内容 |
|---|---|
| `engineering` | 純粋な技術記事（実装・設計・パフォーマンス） |
| `architecture` | アーキテクチャ・設計判断 |
| `devops` | インフラ・CI/CD・運用 |
| `ai-agents` | AIエージェント活用事例 |
| `team` | チーム運営・開発プロセス・採用 |
| `postmortem` | 障害対応・ポストモーテム |

### 主要タグ

`Rails` `React` `TypeScript` `PostgreSQL` `AWS` `ECS` `AI` `MCP` `LLM`
`パフォーマンス` `セキュリティ` `テスト` `CI/CD` `コードレビュー`
`リモートワーク` `採用` `組織`

## 7. AIエージェント間連携

日々の業務で使っている他リポジトリのAIエージェントが、記事化に値する技術的知見を検出した場合、
それをネタとして起票するところから本リポジトリの流れが始まる。

```
業務中の作業・調査（他リポジトリ）
  └─ 記事化に値する技術的知見を検出
       ↓ ネタ起票（非公開リポジトリの ideas/）
       ↓ 書くと決めたら昇格
本リポジトリ articles/drafts/ → articles/<slug>.md → Zenn
```

### 連携時のルール

1. **他リポジトリのAIエージェントは直接本リポジトリにコミットしない** — ネタの起票は指示ベースで行う
2. **記事作成はCTOの指示があった場合のみ** — 自動で草案を量産しない
3. **ネタの提案は積極的に行う** — 「これは記事にできそうです」という提案は歓迎
4. **ネタの段階では本リポジトリに置かない** — public であり、書くと決める前の題材が外から見えるため（§2）

## 8. 重要ルール

1. **記事の作成・草案のコミットはユーザー（CTO）の指示があった場合のみ実行する**
2. **機密情報ガード（§5.4）は絶対に遵守する**
3. **ネタの提案は積極的に行うが、勝手に草案を作成しない**
4. **公開判断は必ずCTOが行う** — AIエージェントが公開可否を判断しない
5. **他リポジトリの機密コードを記事にそのまま転載しない** — 汎化・抽象化した上で掲載する
6. **`published: true` への変更・main への merge はAIエージェントが行わない** — Zenn への公開そのものであり、レビュー承認後に人が実施する
7. **公開済み記事のファイル（`articles/<slug>.md`）を移動・削除しない** — Zenn 側の記事整合が壊れる（[PUBLISHING.md](./PUBLISHING.md) §1）
8. **想定される反論への「回答」をAIが書かない** — 論点を立てて著者に問い返すまでが範囲。判断の中身を代筆すると一般論になる（§5.4.1）
9. **書くと決めていないネタを本リポジトリに置かない** — public なので、`articles/drafts/` に入れた時点で題材が外から見える。判断がついていないものは非公開リポジトリの `ideas/` に置く（§2）
